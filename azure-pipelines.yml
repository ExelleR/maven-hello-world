
# Maven
# Build your Java project and run tests with Apache Maven.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/java

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:

      

- task: Maven@3
  inputs:
    mavenPomFile: './my-app/pom.xml'
    goals: 'compile'
    publishJUnitResults: false
    javaHomeOption: 'JDKVersion'
    mavenVersionOption: 'Default'
    mavenAuthenticateFeed: false
    effectivePomSkip: false
    sonarQubeRunAnalysis: false
    sqMavenPluginVersionChoice: 'latest'

- task: Maven@3
  inputs:
    mavenPomFile: './my-app/pom.xml'
    publishJUnitResults: false
    javaHomeOption: 'JDKVersion'
    mavenVersionOption: 'Default'
    mavenAuthenticateFeed: false
    effectivePomSkip: false
    sonarQubeRunAnalysis: false
- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: |
      # Write your PowerShell commands here.
      
      # Get-ChildItem -Path /home/vsts/work/1/s/my-app/target/classes -Recurse -Directory -Force -ErrorAction SilentlyContinue | Select-Object FullName
      cd /home/vsts/work/1/s/my-app/target/
      ls
      # java -cp /home/vsts/work/1/s/my-app/target/classes/com/mycompany/app/my-app-1.0-SNAPSHOT.jar 
